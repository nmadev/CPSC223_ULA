#include <stdio.h>
#include <stdlib.h>

#include "movies.h"
#include "movie_theater.h"

int main(void) {

    // list of movies
    // for every movie in list of movies
        // create movie struct
        // add movie struct to list of movies

    // get total tickets sold O(n) -> O(1)

}